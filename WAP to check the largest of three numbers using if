#include <stdio.h>

int main() {
    int a,b,c;
    printf("\nenter three number");
    scanf("%d%d%d",&a,&b,&c);
    if(a>b)
    {
       if(a>c)
         {
       printf("\nlargest number is=%d",a);  
         }
    }
    if(b>a)
    {
       if(b>c)
         {
        printf("\nlargest number is=%d",b);
          } 
    }
    if(c>a)
    {
        if(c>b)
        {
        printf("\nlargest number is=%d",c); 
        }
    }
    return 0;
}

